# GSD planning docs — local only
.planning/

# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods / Carthage
Pods/
Carthage/Build/

# Signing — your team id is yours, and Xcode writing it into the project file
# on every device build is how it would otherwise reach a commit.
Config/Local.xcconfig

# Secrets — never commit credentials for the ActiveCommunities account
Secrets.plist
*.xcconfig.local
.env
